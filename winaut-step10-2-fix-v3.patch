diff --git a/apps/api/src/automation-configurations/automation-configurations.service.ts b/apps/api/src/automation-configurations/automation-configurations.service.ts
--- a/apps/api/src/automation-configurations/automation-configurations.service.ts
+++ b/apps/api/src/automation-configurations/automation-configurations.service.ts
@@ -16,7 +16,7 @@
 
 type DatabaseClient = Prisma.TransactionClient | PrismaService['db'];
 
-interface BranchRecord extends AutomationBranchDefinition {
+export interface BranchRecord extends AutomationBranchDefinition {
   active: boolean;
 }
 
diff --git a/apps/api/src/automation-runs/automation-runs.service.spec.ts b/apps/api/src/automation-runs/automation-runs.service.spec.ts
--- a/apps/api/src/automation-runs/automation-runs.service.spec.ts
+++ b/apps/api/src/automation-runs/automation-runs.service.spec.ts
@@ -1,4 +1,4 @@
-import { AutomationDefinitionRegistry } from '../automation-definitions/automation-definition.registry';
+import type { AutomationConfigurationsService } from '../automation-configurations/automation-configurations.service';
 import type { PrismaService } from '../database/prisma.service';
 import { AutomationRunsService } from './automation-runs.service';
 
@@ -18,15 +18,34 @@
     automationCode: '507',
   };
   const create = jest.fn().mockResolvedValue({ id: 'run-id', steps: [] });
+  const findUnique = jest.fn().mockResolvedValue({
+    id: 'run-id',
+    steps: [],
+  });
   const prisma = {
-    db: { automationRun: { create } },
+    db: { automationRun: { create, findUnique } },
   } as unknown as PrismaService;
-  const service = new AutomationRunsService(
-    prisma,
-    new AutomationDefinitionRegistry(),
+  const buildStepsForRun = jest.fn().mockResolvedValue(
+    Array.from({ length: 12 }, (_, index) => ({
+      code: `STEP_${index + 1}`,
+      name: `Etapa ${index + 1}`,
+      sequenceNumber: index + 1,
+      payload: {
+        routine: 507,
+        test: true,
+      },
+    })),
   );
+  const configurations = {
+    buildStepsForRun,
+  } as unknown as AutomationConfigurationsService;
+  const service = new AutomationRunsService(prisma, configurations);
 
-  beforeEach(() => create.mockClear());
+  beforeEach(() => {
+    create.mockClear();
+    findUnique.mockClear();
+    buildStepsForRun.mockClear();
+  });
 
   it('scopes scheduled deduplication to schedule and occurrence', async () => {
     const scheduledFor = new Date('2026-08-17T09:00:00.000Z');
@@ -41,6 +60,11 @@
       'schedule:11111111-1111-4111-8111-111111111111:2026-08-17T09:00:00.000Z',
     );
     expect(call.data.steps.create).toHaveLength(12);
+    expect(buildStepsForRun).toHaveBeenCalledWith(
+      prisma.db,
+      schedule.winthorInstanceId,
+      schedule.automationCode,
+    );
   });
 
   it('uses a distinct key for every manual trigger', async () => {
diff --git a/apps/web/src/features/automation-configurations/automation-configurations-view.tsx b/apps/web/src/features/automation-configurations/automation-configurations-view.tsx
--- a/apps/web/src/features/automation-configurations/automation-configurations-view.tsx
+++ b/apps/web/src/features/automation-configurations/automation-configurations-view.tsx
@@ -1,7 +1,7 @@
 'use client';
 
 import { useQuery } from '@tanstack/react-query';
-import { useEffect, useMemo, useState } from 'react';
+import { useMemo, useState } from 'react';
 
 import { ErrorState } from '@/components/ui/error-state';
 import { winThorInstancesQueryOptions } from '@/features/winthor-instances/queries';
@@ -27,11 +27,7 @@
     [instancesQuery.data],
   );
 
-  useEffect(() => {
-    if (!selectedId && instances.length > 0) {
-      setSelectedId(instances[0].id);
-    }
-  }, [instances, selectedId]);
+  const effectiveSelectedId = selectedId || instances[0]?.id || '';
 
   if (instancesQuery.isPending) {
     return (
@@ -61,7 +57,9 @@
     );
   }
 
-  const selected = instances.find((instance) => instance.id === selectedId);
+  const selected = instances.find(
+    (instance) => instance.id === effectiveSelectedId,
+  );
 
   return (
     <div className="space-y-5">
@@ -74,7 +72,7 @@
         </label>
         <select
           id="configuration-environment"
-          value={selectedId}
+          value={effectiveSelectedId}
           onChange={(event) => setSelectedId(event.target.value)}
           className="mt-2 block w-full rounded-lg border border-[var(--border)] bg-white px-3 py-2.5 text-sm outline-none focus:border-[var(--accent)]"
         >
@@ -95,10 +93,13 @@
         ) : null}
       </section>
 
-      {selectedId ? (
+      {effectiveSelectedId ? (
         <div className="grid gap-5 xl:grid-cols-[minmax(300px,0.75fr)_minmax(0,1.6fr)]">
-          <BranchesPanel winthorInstanceId={selectedId} />
-          <Routine507Panel winthorInstanceId={selectedId} />
+          <BranchesPanel winthorInstanceId={effectiveSelectedId} />
+          <Routine507Panel
+            key={effectiveSelectedId}
+            winthorInstanceId={effectiveSelectedId}
+          />
         </div>
       ) : null}
     </div>
diff --git a/apps/web/src/features/automation-configurations/branch-dialog.tsx b/apps/web/src/features/automation-configurations/branch-dialog.tsx
--- a/apps/web/src/features/automation-configurations/branch-dialog.tsx
+++ b/apps/web/src/features/automation-configurations/branch-dialog.tsx
@@ -5,7 +5,7 @@
   WinThorBranchItem,
 } from '@winaut/contracts';
 import type { FormEvent } from 'react';
-import { useEffect, useState } from 'react';
+import { useState } from 'react';
 
 interface BranchDialogProps {
   open: boolean;
@@ -26,17 +26,8 @@
   onClose,
   onSubmit,
 }: BranchDialogProps) {
-  const [code, setCode] = useState('');
-  const [name, setName] = useState('');
-
-  useEffect(() => {
-    if (!open) {
-      return;
-    }
-
-    setCode(branch?.code ?? '');
-    setName(branch?.name ?? '');
-  }, [branch, open]);
+  const [code, setCode] = useState(branch?.code ?? '');
+  const [name, setName] = useState(branch?.name ?? '');
 
   if (!open) {
     return null;
diff --git a/apps/web/src/features/automation-configurations/branches-panel.tsx b/apps/web/src/features/automation-configurations/branches-panel.tsx
--- a/apps/web/src/features/automation-configurations/branches-panel.tsx
+++ b/apps/web/src/features/automation-configurations/branches-panel.tsx
@@ -157,22 +157,24 @@
         </div>
       ) : null}
 
-      <BranchDialog
-        open={dialogOpen}
-        winthorInstanceId={winthorInstanceId}
-        branch={editing}
-        pending={saveMutation.isPending}
-        error={
-          saveMutation.isError ? saveMutation.error.message : undefined
-        }
-        onClose={() => {
-          if (!saveMutation.isPending) {
-            setDialogOpen(false);
-            setEditing(null);
+      {dialogOpen ? (
+        <BranchDialog
+          open
+          winthorInstanceId={winthorInstanceId}
+          branch={editing}
+          pending={saveMutation.isPending}
+          error={
+            saveMutation.isError ? saveMutation.error.message : undefined
           }
-        }}
-        onSubmit={(input) => saveMutation.mutate(input)}
-      />
+          onClose={() => {
+            if (!saveMutation.isPending) {
+              setDialogOpen(false);
+              setEditing(null);
+            }
+          }}
+          onSubmit={(input) => saveMutation.mutate(input)}
+        />
+      ) : null}
     </section>
   );
 }
diff --git a/apps/web/src/features/automation-configurations/routine-507-panel.tsx b/apps/web/src/features/automation-configurations/routine-507-panel.tsx
--- a/apps/web/src/features/automation-configurations/routine-507-panel.tsx
+++ b/apps/web/src/features/automation-configurations/routine-507-panel.tsx
@@ -5,7 +5,7 @@
   Routine507BranchMode,
   type Routine507Configuration,
 } from '@winaut/contracts';
-import { useEffect, useMemo, useState } from 'react';
+import { useMemo, useState } from 'react';
 
 import { apiClient } from '@/lib/api/client';
 
@@ -37,17 +37,14 @@
   const configurationQuery = useQuery(
     routine507ConfigurationQueryOptions(winthorInstanceId),
   );
-  const [draft, setDraft] = useState<Routine507Configuration | null>(
-    null,
-  );
-
-  useEffect(() => {
-    if (configurationQuery.data) {
-      setDraft(
-        cloneConfiguration(configurationQuery.data.configuration),
-      );
-    }
-  }, [configurationQuery.data]);
+  const [draftOverride, setDraft] =
+    useState<Routine507Configuration | null>(null);
+
+  const draft =
+    draftOverride ??
+    (configurationQuery.data
+      ? cloneConfiguration(configurationQuery.data.configuration)
+      : null);
 
   const saveMutation = useMutation({
     mutationFn: (configuration: Routine507Configuration) =>
